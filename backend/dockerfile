# Официальный Node.js образ в качестве базового
FROM node:16-alpine

# Устанавка в рабочую директорию внутри контейнера
WORKDIR /app

# Копирование package.json и package-lock.json в рабочую директорию
COPY package*.json ./

# Устанавливка зависимостей
RUN npm install

# Копирование всего проекта в рабочую директорию
COPY . .

# Открытие порта 5000 для доступа к приложению
EXPOSE 5000

# Запуск приложения
CMD ["npm", "start"]